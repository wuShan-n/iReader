请彻底审查此计划。针对每个问题或建议，请解释具体的权衡（trade-offs），给出有主见的建议，并在假设方向之前征求我的输入。
我的工程偏好（用这些来指导你的建议）：
* DRY (Don't Repeat Yourself) 很重要——严厉指出重复代码。
* 经过充分测试的代码是不可商榷的；宁愿测试太多，也不要测试太少。
* 我想要**“工程量适度”**的代码——既不是设计不足（脆弱、凑合/hacky），也不是过度设计（过早抽象、不必要的复杂性）。
* 我倾向于处理更多的边缘情况，而不是更少；周全（thoughtfulness） > 速度。
* 偏向显式（explicit）而非巧妙（clever）。
1. 架构审查 (Architecture review)
   评估：
* 整体系统设计和组件边界。
* 依赖图和耦合问题。
* 数据流模式和潜在瓶颈。
* 扩展特性和单点故障。
* 安全架构（鉴权、数据访问、API 边界）。
2. 代码质量审查 (Code quality review)
   评估：
* 代码组织和模块结构。
* DRY 违规——此处请保持严格。
* 错误处理模式和缺失的边缘情况（明确指出这些）。
* 技术债热点。
* 相对于我的偏好，属于过度设计或设计不足的区域。
3. 测试审查 (Test review)
   评估：
* 测试覆盖率缺口（单元测试、集成测试、e2e）。
* 测试质量和断言（assertion）强度。
* 缺失的边缘情况覆盖——务必周全。
* 未测试的失败模式和错误路径。
4. 性能审查 (Performance review)
   评估：
* N+1 查询和数据库访问模式。
* 内存使用问题。
* 缓存机会。
* 慢速或高复杂度的代码路径。
  针对你发现的每个问题：
  （包括 Bug、代码异味/smell、设计疑虑或风险）
* 具体描述问题，附带文件和行号参考。
* 提供 2-3 个选项，在合理的情况下包括“不执行任何操作”。
* 对于每个选项，明确说明：实施工作量、风险、对其他代码的影响以及维护负担。
* 给出你推荐的选项及理由，并对应到上文提到的我的偏好。
* 然后明确询问我是同意还是想选择不同的方向，再继续进行。
  工作流与交互
* 不要主观假设我对时间表或规模的优先级。
* 在完成每个章节的审查后，暂停并征求我的反馈，然后再继续下一部分。
  在开始之前：
  询问我想要以下两个选项中的哪一个：
* 大改动 (BIG CHANGE): 交互式地进行，每次一个部分（架构 -> 代码质量 -> 测试 -> 性能），每个部分最多列出 4 个顶级问题。
* 小改动 (SMALL CHANGE): 交互式地进行，每个审查部分只提一个问题。
  对于每个审查阶段： 输出该阶段问题的解释及优缺点，以及你带主见的建议和理由，然后使用 AskUserQuestion。同时为问题编号并为选项提供字母标记。在使用 AskUserQuestion 时，确保每个选项清楚地标注了问题编号和选项字母，以免用户混淆。始终将推荐选项作为第 1 个选项。