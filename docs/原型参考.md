import React, { useState, useRef } from 'react';
import { 
  ChevronLeft, ChevronRight, ChevronDown, 
  ShoppingCart, MoreVertical, Check, Lock, Edit2, Bookmark, 
  AlignLeft, AlignCenter, AlignRight, AlignJustify,
  Share2, MessageSquare, ThumbsUp, MessageCircle, AlertTriangle, Search,
  Book, Store, Crown, Smile, ArrowUpToLine, FolderPlus, FolderOpen, Trash2, MoreHorizontal
} from 'lucide-react';

const App = () => {
  const contentRef = useRef(null);
  const [currentScreen, setCurrentScreen] = useState('bookshelf'); // 'bookshelf' or 'reader'
  const [isEditMode, setIsEditMode] = useState(false);
  const [selectedBooks, setSelectedBooks] = useState([]);

  const [isUIVisible, setIsUIVisible] = useState(false);
  const [activeTab, setActiveTab] = useState(null); 
  const [openPanel, setOpenPanel] = useState(null); 
  
  const [menuTab, setMenuTab] = useState('toc');
  const [fontTab, setFontTab] = useState('zh');
  const [isNightMode, setIsNightMode] = useState(false);

  const [toggles, setToggles] = useState({
    systemBrightness: true, eyeProtection: false, verticalText: false, 
    traditionalChinese: false, tapLeftToNext: false, fullScreen: true,
    syncReading: true, volumeKeyPage: true
  });

  const toggleSetting = (key) => setToggles(prev => ({ ...prev, [key]: !prev[key] }));

  // --- Mock Data ---
  const myBooks = [
    { id: 1, title: '剑来 (1-54册) 完结精校版', progress: '已读0.1%', coverText: '剑来', coverBg: 'bg-gradient-to-br from-[#8BE4D1] to-[#39A9A4] text-teal-900' },
    { id: 2, title: '必然', progress: '已读91.8%', coverText: '必然', coverBg: 'bg-gradient-to-br from-gray-100 to-gray-300' },
    { id: 3, title: 'Clean Architecture...', progress: '已读22.4%', coverText: 'Clean Architecture', coverBg: 'bg-gradient-to-br from-slate-700 to-slate-900 text-white' },
    { id: 4, title: '分析哲学与人类生活', progress: '已读13.5%', coverText: '分析哲学', coverBg: 'bg-white border border-gray-200 text-gray-800' },
    { id: 5, title: '金蚕往事全集 (14册)', progress: '未读', coverText: '金蚕往事', coverBg: 'bg-gradient-to-br from-yellow-900 to-black text-yellow-500' },
    { id: 6, title: '爱你时有风', progress: '未读', coverText: '爱你时有风', coverBg: 'bg-gradient-to-br from-blue-200 to-blue-400 text-white' },
  ];

  const toggleBookSelection = (id) => {
    setSelectedBooks(prev => prev.includes(id) ? prev.filter(bookId => bookId !== id) : [...prev, id]);
  };

  const handleZoneClick = (e) => {
    if (openPanel) {
      setOpenPanel(null);
      return;
    }
    if (isUIVisible) {
      setIsUIVisible(false);
      setActiveTab(null);
      return;
    }

    const container = contentRef.current;
    if (!container) return;

    const rect = container.getBoundingClientRect();
    const x = e.clientX - rect.left;
    const width = rect.width;

    if (x < width * 0.3) {
      container.scrollBy({ left: -width, behavior: 'smooth' });
    } else if (x > width * 0.7) {
      container.scrollBy({ left: width, behavior: 'smooth' });
    } else {
      setIsUIVisible(true);
    }
  };

  const closeAll = () => {
    setOpenPanel(null);
    setIsUIVisible(false);
    setActiveTab(null);
  };

  // --- UI 组件 ---
  const Toggle = ({ checked, onChange }) => (
    <div 
      className={`w-[44px] h-[24px] flex items-center rounded-full p-[2px] cursor-pointer transition-colors duration-300 ${checked ? 'bg-[#007AFF]' : 'bg-[#E5E5EA]'}`}
      onClick={(e) => { e.stopPropagation(); onChange(); }}
    >
      <div className={`bg-white w-[20px] h-[20px] rounded-full shadow-[0_3px_8px_rgba(0,0,0,0.15)] transform transition-transform duration-300 ${checked ? 'translate-x-[20px]' : 'translate-x-0'}`} />
    </div>
  );

  const BottomSheet = ({ children, onClose, heightClass = "max-h-[85vh]" }) => (
    <div className="absolute inset-0 z-50 flex flex-col justify-end pointer-events-none overflow-hidden pb-4">
      <div className="absolute inset-0 bg-black/20 pointer-events-auto transition-opacity" onClick={onClose} />
      <div className={`bg-[#F8F9FA] rounded-t-[20px] shadow-[0_-8px_30px_rgba(0,0,0,0.12)] w-full pointer-events-auto flex flex-col animate-in slide-in-from-bottom-full duration-300 relative ${heightClass}`}>
        <div className="mt-2 flex-1 overflow-y-auto flex flex-col no-scrollbar">
          {children}
        </div>
      </div>
    </div>
  );

  // --- 完美复刻的 Android 系统状态栏 ---
  const AndroidStatusBar = ({ isDark }) => (
    <div className={`absolute top-0 left-0 right-0 h-8 flex justify-between items-center px-4 z-50 pointer-events-none ${isDark ? 'text-gray-400' : 'text-[#333]'}`}>
      <div className="flex items-center space-x-2.5">
        <span className="text-[12.5px] font-medium tracking-wide">21:12</span>
        <div className="flex items-center space-x-1.5 opacity-80">
          {/* Chrome Icon */}
          <svg viewBox="0 0 24 24" className="w-3.5 h-3.5" fill="currentColor"><path d="M12 2a10 10 0 1010 10A10 10 0 0012 2zm4.8 5.4l-3.2 5.5h-7a7.9 7.9 0 0110.2-5.5zM3.8 13A8 8 0 018.6 4.7l3.6 6.3-4.2 7.3A8 8 0 013.8 13zm8.2 9a8 8 0 01-5-1.7l3.8-6.6h7.6a8 8 0 01-6.4 8.3z"/><circle cx="12" cy="12" r="3.2" fill={isDark ? '#1a1a1a' : 'white'}/></svg>
          {/* WeChat Icon */}
          <svg viewBox="0 0 24 24" className="w-[15px] h-[15px]" fill="currentColor"><path d="M8.5 14.5c-3.6 0-6.5-2.4-6.5-5.5S4.9 3.5 8.5 3.5s6.5 2.4 6.5 5.5-2.9 5.5-6.5 5.5zM17 20.5c-3 0-5.5-2-5.5-4.5S14 11.5 17 11.5s5.5 2 5.5 4.5-2.5 4.5-5.5 4.5z"/></svg>
          {/* Shield Icon */}
          <svg viewBox="0 0 24 24" className="w-3 h-3" fill="currentColor"><path d="M12 1L3 5v6c0 5.5 3.8 10.7 9 12 5.2-1.3 9-6.5 9-12V5l-9-4zm-1 14l-4-4 1.4-1.4 2.6 2.6 6.6-6.6L19 7l-8 8z"/></svg>
        </div>
      </div>
      <div className="flex items-center space-x-1.5 text-[10px] font-medium tracking-tight">
        <span className="mr-1">1.80 KB/s</span>
        {/* Signal Icon */}
        <svg viewBox="0 0 24 24" className="w-[14px] h-[14px] mr-0.5" fill="currentColor"><path d="M2 22h20V2L2 22z"/></svg>
        <span>4G</span>
        <span className="mr-0.5">5G</span>
        <span className="text-[11px] ml-0.5">57%</span>
        {/* Android Battery Icon */}
        <div className="relative flex items-center h-[11px] w-[20px] ml-0.5">
          <div className="w-[18px] h-[10px] border-[1.2px] border-current rounded-[2px] p-[1px] flex items-center">
            <div className="h-full bg-current rounded-[1px]" style={{width: '57%'}}></div>
          </div>
          <div className="w-[1.5px] h-[4px] bg-current rounded-r-[1px]"></div>
        </div>
      </div>
    </div>
  );

  // --- 安卓手势小白条 ---
  const AndroidGestureBar = ({ isDark }) => (
    <div className="absolute bottom-1.5 left-0 right-0 flex justify-center z-50 pointer-events-none">
      <div className={`w-[35%] h-[4px] rounded-full ${isDark ? 'bg-gray-600' : 'bg-[#D1D1D1]'}`}></div>
    </div>
  );

  const TargetIcon = () => (
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" className="w-[22px] h-[22px]">
      <circle cx="12" cy="12" r="8" /><circle cx="12" cy="12" r="2.5" />
    </svg>
  );
  
  const MenuListIcon = () => (
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" className="w-[24px] h-[24px]">
      <path d="M4 6h16M4 12h16M4 18h16"/>
    </svg>
  );

  const LightbulbBoltIcon = () => (
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" className="w-[24px] h-[24px]">
      <path d="M9 18h6M10 21h4" strokeLinecap="round"/>
      <path d="M12 2C8.69 2 6 4.69 6 8c0 2.38 1.4 4.43 3.42 5.42.5.25.83.74.83 1.3V16h3.5v-1.28c0-.56.33-1.05.83-1.3C16.6 12.43 18 10.38 18 8c0-3.31-2.69-6-6-6z"/>
      <path d="M13 5l-2 4h3l-2 4" strokeLinejoin="round"/>
    </svg>
  );

  const MoonIcon = () => (
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" className="w-[24px] h-[24px]">
      <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z" strokeLinejoin="round" strokeLinecap="round"/>
    </svg>
  );

  const SettingsHexIcon = () => (
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" className="w-[24px] h-[24px]">
      <polygon points="12 2 22 8 22 16 12 22 2 16 2 8 12 2" strokeLinejoin="round"/>
      <circle cx="12" cy="12" r="3.5" />
    </svg>
  );

  // --- 书架渲染 ---
  const renderBookshelf = () => (
    <div className="flex flex-col h-full w-full bg-[#F8F9FA] z-40 relative">
      <AndroidStatusBar isDark={false} />
      
      {!isEditMode ? (
        <div className="flex justify-between items-center px-4 py-3 pt-12">
          <h1 className="text-[22px] font-bold text-[#1A1A1A]">书架</h1>
          <div className="flex items-center space-x-4">
            <div className="bg-[#007AFF] text-white text-[11px] px-2.5 py-0.5 rounded-full font-medium shadow-sm">福利</div>
            <Search className="w-[22px] h-[22px] text-[#333]" strokeWidth={2} />
            <MoreHorizontal className="w-[22px] h-[22px] text-[#333] cursor-pointer" strokeWidth={2} onClick={() => setIsEditMode(true)} />
          </div>
        </div>
      ) : (
        <div className="flex justify-between items-center px-4 py-3 pt-12 border-b border-gray-200 bg-white">
          <span className="text-[15px] text-[#666] cursor-pointer" onClick={() => setSelectedBooks(selectedBooks.length === myBooks.length ? [] : myBooks.map(b => b.id))}>全选</span>
          <span className="text-[17px] font-medium text-[#1A1A1A]">已选择{selectedBooks.length}本书</span>
          <span className="text-[15px] text-[#666] cursor-pointer" onClick={() => { setIsEditMode(false); setSelectedBooks([]); }}>完成</span>
        </div>
      )}

      {!isEditMode && (
        <div className="flex justify-between items-center px-4 py-2">
          <div className="bg-white shadow-sm rounded-full px-3 py-1.5 flex items-center text-[12px] text-[#555] font-medium">
            本周读52分钟 <ChevronRight className="w-3.5 h-3.5 ml-0.5 text-[#999]" />
          </div>
          <div className="text-[13px] text-[#999] flex items-center space-x-2">
            <span>筛选</span>
            <span className="text-gray-300">|</span>
            <span onClick={() => setIsEditMode(true)} className="cursor-pointer text-[#1A1A1A]">编辑</span>
          </div>
        </div>
      )}

      <div className="flex-1 overflow-y-auto px-4 py-2 mt-1 no-scrollbar pb-16">
        <div className="grid grid-cols-3 gap-x-5 gap-y-7">
          {myBooks.map(book => (
            <div key={book.id} className="flex flex-col relative cursor-pointer group" onClick={() => {
              if (isEditMode) toggleBookSelection(book.id);
              else setCurrentScreen('reader');
            }}>
              <div className={`w-full aspect-[3/4] ${book.coverBg} rounded-[4px] shadow-[2px_4px_10px_rgba(0,0,0,0.08)] flex items-center justify-center p-3 relative overflow-hidden`}>
                 <span className="text-center font-serif text-[15px] opacity-90 leading-tight" style={{writingMode: book.id === 1 ? 'vertical-rl' : 'horizontal-tb'}}>{book.coverText}</span>
                 {isEditMode && (
                   <div className="absolute bottom-2 right-2">
                     {selectedBooks.includes(book.id) ? (
                       <div className="w-5 h-5 bg-[#007AFF] rounded-full flex items-center justify-center text-white border border-white shadow-sm"><Check className="w-3 h-3" strokeWidth={3} /></div>
                     ) : (
                       <div className="w-5 h-5 bg-black/30 rounded-full border border-white shadow-sm"></div>
                     )}
                   </div>
                 )}
              </div>
              <h3 className="text-[13px] text-[#1A1A1A] mt-3 leading-[1.3] line-clamp-2 font-medium">{book.title}</h3>
              <p className="text-[11px] text-[#999] mt-1">{book.progress}</p>
            </div>
          ))}
        </div>
      </div>

      {!isEditMode ? (
        <div className="flex justify-around items-center py-2 pb-6 border-t border-gray-200 bg-[#F8F9FA] shadow-[0_-2px_10px_rgba(0,0,0,0.02)]">
          <div className="flex flex-col items-center text-[#007AFF]"><Book className="w-6 h-6 mb-1" strokeWidth={2} /><span className="text-[10px] font-medium">书架</span></div>
          <div className="flex flex-col items-center text-[#999]"><Store className="w-6 h-6 mb-1" strokeWidth={1.5} /><span className="text-[10px]">书城</span></div>
          <div className="flex flex-col items-center text-[#999]"><Crown className="w-6 h-6 mb-1" strokeWidth={1.5} /><span className="text-[10px]">会员</span></div>
          <div className="flex flex-col items-center text-[#999]"><Smile className="w-6 h-6 mb-1" strokeWidth={1.5} /><span className="text-[10px]">我的</span></div>
        </div>
      ) : (
        <div className="flex justify-around items-center py-3 pb-6 border-t border-gray-200 bg-[#F8F9FA] shadow-[0_-2px_10px_rgba(0,0,0,0.02)]">
          <div className="flex flex-col items-center text-[#666]"><ArrowUpToLine className="w-[22px] h-[22px] mb-1" strokeWidth={1.5} /><span className="text-[10px]">置顶</span></div>
          <div className="flex flex-col items-center text-[#666]"><FolderPlus className="w-[22px] h-[22px] mb-1" strokeWidth={1.5} /><span className="text-[10px]">加入书单</span></div>
          <div className="flex flex-col items-center text-[#666]"><FolderOpen className="w-[22px] h-[22px] mb-1" strokeWidth={1.5} /><span className="text-[10px]">移动至</span></div>
          <div className="flex flex-col items-center text-[#666]"><Share2 className="w-[22px] h-[22px] mb-1" strokeWidth={1.5} /><span className="text-[10px]">分享</span></div>
          <div className="flex flex-col items-center text-[#FF3B30]"><Trash2 className="w-[22px] h-[22px] mb-1" strokeWidth={1.5} /><span className="text-[10px]">删除</span></div>
          <div className="flex flex-col items-center text-[#666]"><MoreHorizontal className="w-[22px] h-[22px] mb-1" strokeWidth={1.5} /><span className="text-[10px]">更多</span></div>
        </div>
      )}
      <AndroidGestureBar isDark={false} />
    </div>
  );

  // --- 内部面板内容 (嵌入在底部导航栏之上) ---
  const renderMenuContent = () => (
    <div className="h-[65vh] flex flex-col bg-[#F8F9FA] animate-in slide-in-from-bottom-10 duration-200">
      <div className="px-5 pt-4 pb-2 border-b border-gray-200 flex items-center justify-between shrink-0">
        <div className="text-[17px] font-medium flex items-center text-[#1A1A1A]">
          剑来 (1-54册) 完结精校版 <ChevronRight className="w-4 h-4 ml-1 text-gray-400" />
        </div>
      </div>
      <div className="px-5 py-3 shrink-0">
        <div className="flex bg-[#E9ECEF] rounded-full p-[3px]">
          {['toc', 'notes', 'bookmarks'].map((tab) => (
            <button key={tab} className={`flex-1 py-1.5 text-[15px] rounded-full transition-all ${menuTab === tab ? 'bg-white text-[#1A1A1A] font-medium shadow-sm' : 'text-[#6C757D]'}`} onClick={() => setMenuTab(tab)}>
              {{ toc: '目录', notes: '笔记', bookmarks: '书签' }[tab]}
            </button>
          ))}
        </div>
      </div>
      <div className="flex-1 overflow-y-auto no-scrollbar">
        {menuTab === 'toc' && (
          <div className="px-5 pb-6">
            <div className="flex justify-between items-center text-[13px] text-[#6C757D] py-3 border-b border-gray-200">
              <span>已完结 共593章</span>
              <div className="flex space-x-6"><span>下载</span><span>正序</span></div>
            </div>
            <div className="py-4 flex items-center text-[15px] text-[#1A1A1A] font-medium">
              <ChevronDown className="w-4 h-4 mr-1 text-[#1A1A1A]" strokeWidth={2}/> 剑来 (1-21册)
            </div>
            <ul className="space-y-6 py-2 pl-5">
              {["第二章 稗草", "第三章 少女和飞剑", "第四章 捕蛇鹰", "第五章 离别", "第六章 敲山", "第七章 拳谱", "第八章 少年和老狗", "第九章 天行健"].map((ch, i) => (
                <li key={i} className={`text-[15px] pb-5 border-b border-gray-200 ${i===0 ? 'text-[#007AFF] font-medium' : 'text-[#495057]'}`}>{ch}</li>
              ))}
            </ul>
          </div>
        )}
      </div>
    </div>
  );

  const renderBrightnessContent = () => (
    <div className="px-6 pt-6 pb-4 flex flex-col bg-[#F8F9FA] animate-in slide-in-from-bottom-10 duration-200">
      <div className="flex items-center space-x-4">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" className="w-5 h-5 text-[#ADB5BD]"><circle cx="12" cy="12" r="5"/><path d="M12 1v2M12 21v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"/></svg>
        <div className="flex-1 relative h-6 flex items-center">
          <input type="range" className="w-full absolute opacity-0 cursor-pointer z-10" defaultValue={30} />
          <div className="w-full h-1.5 bg-[#DEE2E6] rounded-full overflow-hidden">
             <div className="h-full bg-[#007AFF]" style={{width: `30%`}}></div>
          </div>
          <div className="w-[22px] h-[22px] bg-white rounded-full shadow-[0_2px_5px_rgba(0,0,0,0.2)] absolute pointer-events-none flex items-center justify-center" style={{left: `calc(30% - 11px)`}}></div>
        </div>
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" className="w-6 h-6 text-[#1A1A1A]"><circle cx="12" cy="12" r="5"/><path d="M12 1v2M12 21v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"/></svg>
      </div>
      <div className="flex justify-between items-center px-1 mt-8">
        <div className="flex items-center space-x-3">
          <span className="text-[15px] text-[#1A1A1A]">系统亮度</span>
          <Toggle checked={toggles.systemBrightness} onChange={() => toggleSetting('systemBrightness')} />
        </div>
        <div className="flex items-center space-x-3">
          <span className="text-[15px] text-[#1A1A1A]">护眼模式</span>
          <Toggle checked={toggles.eyeProtection} onChange={() => toggleSetting('eyeProtection')} />
        </div>
      </div>
    </div>
  );

  const renderSettingsContent = () => (
    <div className="px-5 pt-6 pb-4 flex flex-col space-y-5 bg-[#F8F9FA] animate-in slide-in-from-bottom-10 duration-200">
      <div className="flex items-center space-x-3">
        <div className="flex items-center bg-[#E9ECEF] rounded-[8px] h-[40px] px-1 flex-1">
          <button className="flex-1 text-[17px] text-[#1A1A1A] font-medium h-full rounded-[6px] active:bg-[#DEE2E6] transition-colors">A-</button>
          <span className="text-[#6C757D] text-[13px] w-8 text-center">68</span>
          <button className="flex-1 text-[17px] text-[#1A1A1A] font-medium h-full rounded-[6px] active:bg-[#DEE2E6] transition-colors">A+</button>
        </div>
        <button className="flex-[0.8] bg-[#E9ECEF] rounded-[8px] h-[40px] text-[15px] text-[#1A1A1A] active:bg-[#DEE2E6] transition-colors" onClick={() => setOpenPanel('sub_font')}>
          系统字体
        </button>
      </div>
      <div className="flex items-center space-x-3">
        <div className="flex items-center space-x-2 flex-[1.2]">
          {[AlignLeft, AlignCenter, AlignRight, AlignJustify].map((Icon, i) => (
            <button key={i} className={`flex-1 h-[40px] flex justify-center items-center rounded-[8px] ${i === 2 ? 'bg-white border-[1.5px] border-[#007AFF] text-[#007AFF]' : 'bg-[#E9ECEF] text-[#495057]'}`}>
              <Icon className="w-[18px] h-[18px]" strokeWidth={2.5} />
            </button>
          ))}
        </div>
        <button className="flex-[0.8] bg-[#E9ECEF] rounded-[8px] h-[40px] text-[15px] text-[#1A1A1A] active:bg-[#DEE2E6]" onClick={() => setOpenPanel('sub_spacing')}>
          更多间距
        </button>
      </div>
      <div className="flex items-center space-x-3">
        <div className="flex justify-between flex-[1.2] px-1">
          <div className="w-[34px] h-[34px] rounded-full bg-white border border-[#DEE2E6] shadow-sm relative"><div className="absolute inset-0 border-2 border-[#007AFF] rounded-full scale-110"></div></div>
          <div className="w-[34px] h-[34px] rounded-full bg-[#F3E7CA]"></div>
          <div className="w-[34px] h-[34px] rounded-full bg-[#DFD4C5]"></div>
          <div className="w-[34px] h-[34px] rounded-full bg-[#CCE0D1]"></div>
          <div className="w-[34px] h-[34px] rounded-full bg-[#2C2C2C]"></div>
        </div>
        <button className="flex-[0.8] bg-white border-[1.5px] border-[#DEE2E6] rounded-[8px] h-[40px] text-[15px] text-[#1A1A1A]" onClick={() => setOpenPanel('sub_morebg')}>
          更多背景
        </button>
      </div>
      <div className="flex justify-between items-center text-[12px] text-[#6C757D] px-1 pt-1">
        <span className="flex items-center cursor-pointer">自动阅读 <ChevronRight className="w-3.5 h-3.5 ml-0.5" /></span>
        <span className="flex items-center cursor-pointer" onClick={() => setOpenPanel('sub_pageturn')}>左右覆盖 <ChevronRight className="w-3.5 h-3.5 ml-0.5" /></span>
        <span className="flex items-center cursor-pointer" onClick={() => setOpenPanel('full_settings')}>更多设置 <ChevronRight className="w-3.5 h-3.5 ml-0.5" /></span>
      </div>
    </div>
  );

  return (
    <div className="flex justify-center items-center min-h-screen bg-[#E5E5E5] sm:p-4 selection:bg-[#B3D4FF] selection:text-[#333]">
      <div className="relative w-full h-[100dvh] sm:h-[844px] sm:w-[390px] sm:rounded-[40px] sm:border-[12px] sm:border-[#212529] shadow-[0_24px_60px_rgba(0,0,0,0.25)] overflow-hidden bg-white flex flex-col font-sans">
        
        {currentScreen === 'bookshelf' ? renderBookshelf() : (
        <div className={`relative h-full w-full overflow-hidden ${isNightMode ? 'bg-[#121212] text-[#ADB5BD]' : 'bg-[#FDF9F3] text-[#212529]'}`}>
          
          <AndroidStatusBar isDark={isNightMode} />

          {/* 左右滑动分页区域 */}
          <div 
            ref={contentRef}
            className="h-full w-full pt-[55px] pb-[40px] px-5 overflow-x-auto overflow-y-hidden snap-x snap-mandatory flex no-scrollbar"
            onClick={handleZoneClick}
          >
            <div 
              className="h-full shrink-0"
              style={{
                 columnWidth: 'calc(min(100vw, 390px) - 40px)',
                 columnGap: '40px',
                 height: '100%'
              }}
            >
              <p className="text-[11px] text-[#ADB5BD] mb-8 tracking-widest font-light opacity-80" style={{breakInside: 'avoid'}}>第二章 稗草</p>
              
              <div className="space-y-[1.8em] text-[20px] leading-[1.85] tracking-[1px] text-justify font-serif" style={{textIndent: '2em'}}>
                <p>来就能有一把真剑，就忍不住兴奋起来，丢了树枝，开始边跑边喊。</p>
                <p>刘羡阳想着阮师傅私下传授的那几个拳架子，就开始练习起来，倒也有模有样，虎虎生风。</p>
                <p>刘羡阳与廊桥越来越近。廊桥北端的台阶上，坐着四个人：姿态婀娜的丰腴美妇，怀里抱着一个身穿大红袍子的男孩，男孩高高扬起下巴，像是一位刚刚获得大捷的将军；台阶那一头，坐着个满头霜雪的高大老人，老人正在小声安慰一个气鼓鼓的小女孩。小女孩粉雕玉琢，宛如世上最精巧的瓷娃娃，她的稚嫩肌肤在阳光照耀下晶莹剔透，以至于能够清晰看到皮肤下的一条条青筋脉络。</p>
                <p>两个孩子刚刚吵完架，小女孩泫然欲泣，小男孩愈发得意。</p>
                <p>陈平安坐在小板凳上，望着那个名叫宋集薪的同龄人，他有着一双极为好看的眼眸，哪怕是生气的时候，也让人觉得他在笑。陈平安低头看了看自己的草鞋，又抬头看了看宋集薪脚上那双纤尘不染的绸缎靴子，突然觉得很有意思。</p>
                <p>“看什么看？泥腿子！”宋集薪恶狠狠地瞪了一眼陈平安。陈平安也不恼，只是咧嘴一笑，露出雪白的牙齿。这让宋集薪更生气了，仿佛一拳打在了棉花上。</p>
                <p>老人看着这两个孩子，无奈地摇了摇头。这世道，最不缺的就是心高气傲的少年郎，可真能走得长远的，往往是那些懂得低头看路的人。</p>
              </div>
            </div>
          </div>

          {/* === 纯净阅读模式：只保留手势小白条 === */}
          {!isUIVisible && <AndroidGestureBar isDark={isNightMode} />}

          {/* === 操作导航栏层 === */}
          {isUIVisible && (
            <div className="absolute inset-0 z-40 flex flex-col justify-between pointer-events-none">
              
              {/* 顶部 Action Bar */}
              <div className={`h-[85px] flex items-end justify-between px-2 pb-3 pointer-events-auto shadow-[0_2px_10px_rgba(0,0,0,0.05)] animate-in slide-in-from-top-full duration-200 ${isNightMode ? 'bg-[#1E1E1E]' : 'bg-[#F8F9FA]'}`}>
                <div className="w-10 h-10 flex items-center justify-center cursor-pointer" onClick={() => setCurrentScreen('bookshelf')}>
                  <ChevronLeft className={`w-7 h-7 ${isNightMode ? 'text-gray-300' : 'text-[#1A1A1A]'}`} strokeWidth={1.5} />
                </div>
                <div className="flex space-x-1">
                   <div className="w-10 h-10 flex items-center justify-center cursor-pointer"><ShoppingCart className={`w-[22px] h-[22px] ${isNightMode ? 'text-gray-300' : 'text-[#1A1A1A]'}`} strokeWidth={1.5} /></div>
                   <div className="w-10 h-10 flex items-center justify-center cursor-pointer"><TargetIcon /></div>
                   <div className="w-10 h-10 flex items-center justify-center cursor-pointer" onClick={() => setOpenPanel('reader_more')}><MoreVertical className={`w-6 h-6 ${isNightMode ? 'text-gray-300' : 'text-[#1A1A1A]'}`} strokeWidth={1.5} /></div>
                </div>
              </div>

              <div className="flex-1 pointer-events-auto" onClick={closeAll}></div>

              {/* 底部导航与内部面板区 */}
              <div className={`pointer-events-auto rounded-t-[20px] shadow-[0_-10px_40px_rgba(0,0,0,0.1)] flex flex-col transform transition-transform duration-300 ${isNightMode ? 'bg-[#1E1E1E]' : 'bg-[#F8F9FA]'}`}>
                
                {activeTab === 'menu' && renderMenuContent()}
                {activeTab === 'brightness' && renderBrightnessContent()}
                {activeTab === 'settings' && renderSettingsContent()}

                <div className={`flex justify-between px-2 pt-2 pb-7 sm:pb-6 border-t z-10 ${isNightMode ? 'bg-[#1E1E1E] border-gray-800' : 'bg-[#F8F9FA] border-[#DEE2E6]'}`}>
                  {[
                    { id: 'menu', label: '目录', Icon: MenuListIcon },
                    { id: 'brightness', label: '亮度', Icon: LightbulbBoltIcon },
                    { id: 'night', label: isNightMode ? '日间' : '夜间', Icon: MoonIcon, action: () => setIsNightMode(!isNightMode) },
                    { id: 'settings', label: '设置', Icon: SettingsHexIcon }
                  ].map(btn => (
                    <button 
                      key={btn.id}
                      className={`flex flex-col items-center justify-center w-1/4 py-1.5 space-y-1.5 transition-colors ${activeTab === btn.id ? 'text-[#007AFF] font-medium' : isNightMode ? 'text-[#ADB5BD]' : 'text-[#6C757D]'}`} 
                      onClick={() => btn.action ? btn.action() : setActiveTab(activeTab === btn.id ? null : btn.id)}
                    >
                      <btn.Icon />
                      <span className="text-[10px]">{btn.label}</span>
                    </button>
                  ))}
                </div>
                {/* 呼出菜单时也需要显示手势条 */}
                <AndroidGestureBar isDark={isNightMode} />
              </div>
            </div>
          )}

          {/* === 独立二级 BottomSheet 面板层 === */}
          {openPanel === 'sub_font' && (
            <BottomSheet onClose={() => setOpenPanel(null)} heightClass="h-[70vh]">
              <div className="px-4 py-3 border-b border-[#DEE2E6] flex items-center relative shrink-0">
                <ChevronDown className="w-6 h-6 text-[#6C757D] cursor-pointer absolute left-4" onClick={() => setOpenPanel(null)} />
                <div className="w-full flex justify-center space-x-10 text-[15px]">
                   <span className={`${fontTab === 'zh' ? 'text-[#1A1A1A] font-bold' : 'text-[#ADB5BD]'}`} onClick={() => setFontTab('zh')}>中文字体</span>
                   <span className={`${fontTab === 'en' ? 'text-[#1A1A1A] font-bold' : 'text-[#ADB5BD]'}`} onClick={() => setFontTab('en')}>英文字体</span>
                </div>
              </div>
              <div className="p-4 grid grid-cols-2 gap-3 pb-8">
                  {[{ name: "系统字体", selected: true }, { name: "方正新楷体", size: "23M" }, { name: "霞鹜文楷", size: "18.01MB" }, { name: "思源宋体", size: "22.76MB" }, { name: "方正聚珍新仿", size: "15.12MB", tag: "会员专享·可试用" }, { name: "方正悠宋", size: "12.92MB", tag: "会员专享·可试用" }].map((f, i) => (
                    <div key={i} className={`h-[72px] p-3 rounded-[8px] flex flex-col justify-between relative ${f.selected ? 'bg-[#EBF5FF] border-[1.5px] border-[#007AFF]' : 'bg-white border border-[#DEE2E6]'}`}>
                      <div className={`text-[15px] ${f.selected ? 'text-[#007AFF] font-medium' : 'text-[#1A1A1A]'}`}>{f.name}</div>
                      <div className="flex justify-between items-end w-full">
                        {f.tag && <span className="text-[10px] text-[#F59F00]">{f.tag}</span>}
                        <span className="text-[10px] text-[#ADB5BD] ml-auto">{f.size}</span>
                      </div>
                      {f.selected && <Check className="w-4 h-4 absolute top-3 right-3 text-[#007AFF]" strokeWidth={3} />}
                    </div>
                  ))}
              </div>
            </BottomSheet>
          )}

          {openPanel === 'sub_spacing' && (
            <BottomSheet onClose={() => setOpenPanel(null)} heightClass="h-[360px]">
              <div className="flex items-center justify-between px-4 py-3 border-b border-[#DEE2E6] shrink-0">
                <ChevronDown className="w-6 h-6 text-[#6C757D] cursor-pointer" onClick={() => setOpenPanel(null)} />
                <span className="text-[15px] font-medium text-[#1A1A1A]">间距设置</span>
                <span className="text-[13px] text-[#FF4949]">还原上次设置</span>
              </div>
              <div className="px-6 py-6 space-y-[26px]">
                {['行间距', '段间距', '上间距', '下间距', '左右间距'].map((label, i) => (
                  <div key={i} className="flex items-center">
                    <span className="text-[13px] text-[#495057] w-16">{label}</span>
                    <div className="flex-1 relative h-6 flex items-center">
                      <input type="range" className="w-full absolute opacity-0 cursor-pointer z-10" defaultValue={60 + (i === 1 ? -10 : 0)} />
                      <div className="w-full h-1.5 bg-[#DEE2E6] rounded-full overflow-hidden">
                         <div className="h-full bg-[#007AFF]" style={{width: `${60 + (i === 1 ? -10 : 0)}%`}}></div>
                      </div>
                      <div className="w-[20px] h-[20px] bg-white rounded-full shadow-[0_2px_4px_rgba(0,0,0,0.2)] absolute pointer-events-none" style={{left: `calc(${60 + (i === 1 ? -10 : 0)}% - 10px)`}}></div>
                    </div>
                  </div>
                ))}
              </div>
            </BottomSheet>
          )}

          {openPanel === 'sub_pageturn' && (
            <BottomSheet onClose={() => setOpenPanel(null)} heightClass="h-[220px]">
              <div className="flex items-center justify-between px-4 py-3 border-b border-[#DEE2E6] shrink-0">
                <ChevronDown className="w-6 h-6 text-[#6C757D] cursor-pointer" onClick={() => setOpenPanel(null)} />
                <span className="text-[15px] font-medium text-[#1A1A1A]">选择翻页方式</span>
                <div className="w-6"></div>
              </div>
              <div className="px-6 py-6 flex justify-between">
                {[
                  { name: "仿真翻页", svg: <svg viewBox="0 0 24 24" fill="none" className="w-8 h-8 text-[#CED4DA]"><path d="M16 4h-8a2 2 0 00-2 2v12a2 2 0 002 2h8a2 2 0 002-2v-5l-4-4h4V6a2 2 0 00-2-2z" stroke="currentColor" strokeWidth="1.5"/><path d="M14 9l4 4" stroke="currentColor" strokeWidth="1.5"/></svg> },
                  { name: "左右覆盖", selected: true, svg: <svg viewBox="0 0 24 24" fill="none" className="w-8 h-8 text-[#007AFF]"><rect x="4" y="4" width="12" height="16" rx="1" stroke="currentColor" strokeWidth="1.5"/><rect x="8" y="4" width="12" height="16" rx="1" stroke="currentColor" strokeWidth="1.5"/><path d="M12 14l-2-2 2-2" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"/></svg> },
                  { name: "上下滑动", svg: <svg viewBox="0 0 24 24" fill="none" className="w-8 h-8 text-[#CED4DA]"><rect x="5" y="4" width="14" height="16" rx="1" stroke="currentColor" strokeWidth="1.5"/><path d="M12 8v8M10 10l2-2 2 2M10 14l2 2 2-2" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"/></svg> },
                  { name: "无动效", svg: <svg viewBox="0 0 24 24" fill="none" className="w-8 h-8 text-[#CED4DA]"><rect x="5" y="4" width="14" height="16" rx="1" stroke="currentColor" strokeWidth="1.5"/><path d="M9 8h6M9 12h6M9 16h4" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round"/></svg> },
                ].map((pt, i) => (
                  <div key={i} className="flex flex-col items-center space-y-3">
                    <div className={`w-[66px] h-[86px] rounded-[8px] flex items-center justify-center bg-white shadow-sm ${pt.selected ? 'border-[1.5px] border-[#007AFF]' : 'border border-[#DEE2E6]'}`}>
                      {pt.svg}
                    </div>
                    <span className={`text-[12px] ${pt.selected ? 'text-[#007AFF] font-medium' : 'text-[#6C757D]'}`}>{pt.name}</span>
                  </div>
                ))}
              </div>
            </BottomSheet>
          )}

          {openPanel === 'sub_morebg' && (
            <BottomSheet onClose={() => setOpenPanel(null)} heightClass="h-[60vh]">
              <div className="flex items-center justify-between px-4 py-3 border-b border-[#DEE2E6] shrink-0">
                <ChevronDown className="w-6 h-6 text-[#6C757D] cursor-pointer" onClick={() => setOpenPanel(null)} />
                <span className="text-[15px] font-medium text-[#1A1A1A]">更多背景</span>
                <span className="text-[13px] text-[#FF4949]">还原上次设置</span>
              </div>
              <div className="px-5 py-5 space-y-8 overflow-y-auto no-scrollbar">
                <div>
                  <h4 className="text-[12px] text-[#6C757D] mb-4">字体颜色</h4>
                  <div className="grid grid-cols-6 gap-x-2 gap-y-3">
                     <div className="aspect-square rounded-full bg-[#354038] border-[2px] border-[#007AFF]"></div>
                     <div className="aspect-square rounded-full bg-[#4A3D42]"></div>
                     <div className="aspect-square rounded-full bg-[#4B433E]"></div>
                     <div className="aspect-square rounded-full bg-[#4F5155]"></div>
                     <div className="aspect-square rounded-full bg-[#2A2E3B]"></div>
                     <div className="aspect-square rounded-full bg-[#3A1E1C]"></div>
                     <div className="aspect-square rounded-full bg-[#E5E5D8]"></div>
                     <div className="aspect-square rounded-full bg-[#C9D9D3]"></div>
                     <div className="aspect-square rounded-full bg-[#B2D1D6]"></div>
                     <div className="aspect-square rounded-full bg-[#BFC2C6]"></div>
                     <div className="aspect-square rounded-full bg-[#83858A]"></div>
                     <div className="aspect-square rounded-full bg-[#5D5757]"></div>
                  </div>
                </div>
                <div>
                  <h4 className="text-[12px] text-[#6C757D] mb-4">背景颜色</h4>
                  <div className="grid grid-cols-6 gap-x-2 gap-y-3">
                     {["#8E6F74","#83786B","#6A6D72","#273A2A","#2C2E33","#48191D","#655E57",
                       "#F1E6D3","#DFDEC5","#CADBCF","#BDC8AE","#D7B7B3","#557980","#27313F",
                       "#4C5248","#F4F4F4","#E0D4B2","#A9A293"].map((c, i) => (
                       <div key={i} className="aspect-square rounded-full border border-black/5" style={{backgroundColor: c}}></div>
                     ))}
                  </div>
                </div>
              </div>
            </BottomSheet>
          )}

          {openPanel === 'reader_more' && (
            <BottomSheet onClose={() => setOpenPanel(null)} heightClass="h-[360px]">
              <div className="px-5 py-4 flex items-center border-b border-[#DEE2E6] shrink-0">
                 <div className="w-11 h-16 bg-gradient-to-b from-[#8BE4D1] to-[#39A9A4] rounded-[4px] shadow-[2px_3px_8px_rgba(0,0,0,0.1)] mr-4 flex-shrink-0 flex items-center justify-center relative overflow-hidden">
                    <span className="text-[18px] font-serif text-black opacity-80" style={{writingMode: 'vertical-rl'}}>剑来</span>
                 </div>
                 <div className="flex-1">
                   <h3 className="text-[16px] text-[#1A1A1A] font-medium">剑来 (1-54册) 完结精校版</h3>
                   <p className="text-[12px] text-[#6C757D] mt-1">烽火戏诸侯</p>
                 </div>
                 <div className="text-[12px] text-[#ADB5BD] flex items-center cursor-pointer">查看详情 <ChevronRight className="w-3.5 h-3.5 ml-0.5" /></div>
              </div>
              <div className="px-6 py-5 grid grid-cols-4 gap-y-6">
                {[
                  {icon: Bookmark, label: "添加书签"}, {icon: Share2, label: "分享"}, {icon: Search, label: "全文搜索"}, 
                  {icon: MessageCircle, label: "书圈", badge: "3"},
                  {icon: null, svg: <svg viewBox="0 0 24 24" fill="none" className="w-6 h-6"><rect x="4" y="6" width="16" height="12" rx="2" stroke="currentColor" strokeWidth="2"/><path d="M8 10h8M8 14h4" stroke="currentColor" strokeWidth="2" strokeLinecap="round"/></svg>, label: "投票"},
                  {icon: MessageSquare, label: "显示想法", active: true}, {icon: AlertTriangle, label: "举报"}
                ].map((item, i) => (
                  <div key={i} className="flex flex-col items-center space-y-2 cursor-pointer relative group">
                    <div className={`w-[52px] h-[52px] rounded-[16px] flex items-center justify-center transition-colors ${item.active ? 'bg-[#EBF5FF] text-[#007AFF]' : 'bg-white text-[#495057] shadow-sm border border-[#DEE2E6]'}`}>
                      {item.svg || (item.icon && <item.icon className="w-6 h-6" strokeWidth={1.5}/>)}
                    </div>
                    {item.badge && <div className="absolute -top-1.5 right-1.5 bg-[#FF3B30] text-white text-[10px] font-medium min-w-[16px] h-[16px] flex items-center justify-center rounded-full px-1 border-[1.5px] border-white">{item.badge}</div>}
                    <span className={`text-[11px] ${item.active ? 'text-[#007AFF] font-medium' : 'text-[#6C757D]'}`}>{item.label}</span>
                  </div>
                ))}
              </div>
              <div className="mt-auto border-t border-[#DEE2E6] text-center text-[16px] text-[#495057] py-3.5 cursor-pointer bg-white" onClick={() => setOpenPanel(null)}>取消</div>
            </BottomSheet>
          )}

          {/* 全屏的更多阅读设置页 (安卓风格) */}
          {openPanel === 'full_settings' && (
            <div className="absolute inset-0 bg-[#F8F9FA] z-50 flex flex-col animate-in slide-in-from-right duration-300">
              <AndroidStatusBar isDark={false} />
              <div className="bg-[#F8F9FA] flex items-center px-4 py-3 shrink-0 pt-12 shadow-sm">
                <ChevronLeft className="w-7 h-7 text-[#1A1A1A] cursor-pointer" strokeWidth={1.5} onClick={() => setOpenPanel(null)} />
                <h1 className="text-[18px] font-medium ml-2 text-[#1A1A1A]">阅读设置</h1>
              </div>
              <div className="flex-1 overflow-y-auto no-scrollbar">
                <div className="px-5 py-2.5 text-[12px] font-medium text-[#007AFF]">屏幕显示</div>
                <div className="bg-white border-y border-[#DEE2E6]">
                  <div className="flex justify-between items-center p-4 pl-5 border-b border-[#DEE2E6]">
                    <span className="text-[16px] text-[#1A1A1A]">阅读进度显示</span>
                    <span className="text-[14px] text-[#6C757D] flex items-center">本章 百分比 <ChevronRight className="w-4 h-4 ml-1 text-[#ADB5BD]" /></span>
                  </div>
                  <div className="flex justify-between items-center p-4 pl-5">
                    <span className="text-[16px] text-[#1A1A1A]">全屏模式</span>
                    <Toggle checked={toggles.fullScreen} onChange={() => toggleSetting('fullScreen')} />
                  </div>
                </div>
              </div>
              <AndroidGestureBar isDark={false} />
            </div>
          )}

        </div>
        )}
      </div>
    </div>
  );
};

export default App;